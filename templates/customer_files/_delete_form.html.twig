<form method="post" action="{{ path('customer_files_delete', {'id': customer_file.id}) }}" onsubmit="return confirm('Attention ! Êtes-vous sûr de vouloir supprimer cet objet ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer_file.id) }}">
    <button class="btn">Delete</button>
</form>
