{% extends 'base.html.twig' %}

{% block title %}ClientStatut{% endblock %}
{% set title = client_statut.name %}
{% block header %}
    <a href="{{ path('client_statut_index') }}" class="btn btn-info">Retour</a>
{% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ client_statut.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ client_statut.name }}</td>
            </tr>
            <tr>
                <th>Créer le :</th>
                <td>{{ client_statut.createdAt ? client_statut.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modifier le :</th>
                <td>{{ client_statut.updatedAt ? client_statut.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mt-3">
    <a href="{{ path('client_statut_edit', {'id': client_statut.id}) }}" class="btn btn-info">Modifier</a>

    {{ include('client_statut/_delete_form.html.twig') }}
    </div>
    
{% endblock %}

