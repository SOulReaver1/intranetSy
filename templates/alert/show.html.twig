{% extends 'base.html.twig' %}

{% set title = alert.name %}
{% block header %}
       <a href="{{ path('alert_index') }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th># :</th>
                <td>{{ alert.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ alert.name }}</td>
            </tr>
            <tr>
                <th>Créer le :</th>
                <td>{{ alert.createdAt ? alert.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modifier le :</th>
                <td>{{ alert.updatedAt ? alert.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col mt-4">
            <div class="row">
                <a href="{{ path('alert_edit', {'id': alert.id}) }}" class="btn btn-primary">Modifier</a>

                {{ include('alert/_delete_form.html.twig') }}
            </div>
        </div>
{% endblock %}
