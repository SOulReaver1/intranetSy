{% extends 'base.html.twig' %}

{% block title %}Provider{% endblock %}
{% set title = provider.name %}
{% block header %}
    <a href="{{ path('provider_index') }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ provider.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ provider.name }}</td>
            </tr>
            <tr>
                <th>Créer le :</th>
                <td>{{ provider.createdAt ? provider.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modifier le :</th>
                <td>{{ provider.updatedAt ? provider.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col mt-4">
        <div class="row">
            <a href="{{ path('provider_edit', {'id': provider.id}) }}" class="btn btn-primary">Modifier</a>

            {{ include('provider/_delete_form.html.twig') }}
        </div>
    </div>
    
{% endblock %}
