{% extends 'base.html.twig' %}

{% block title %}ProviderParam{% endblock %}
{% set title = provider_param.name %}
{% block header %}
    <a href="{{ path('provider_param_index') }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ provider_param.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ provider_param.name }}</td>
            </tr>
            <tr>
                <th>Créer le :</th>
                <td>{{ provider_param.createdAt ? provider_param.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modifier le :</th>
                <td>{{ provider_param.updatedAt ? provider_param.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col mt-4"> 
        <div class="row">
            <a href="{{ path('provider_param_edit', {'id': provider_param.id}) }}" class="btn btn-primary">Modifier</a>

            {{ include('provider_param/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
