{% extends 'base.html.twig' %}

{% block title %}CustomerFilesStatut{% endblock %}
{% set title = customer_files_statut.name %}
{% block header %}
    <a href="{{ path('customer_files_statut_index') }}" class="btn btn-info">Retour</a>
{% endblock %}
{% block body %}
<div class="row">
    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ customer_files_statut.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ customer_files_statut.name }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ customer_files_statut.createdAt ? customer_files_statut.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <div class="row mt-3">
        <a href="{{ path('customer_files_statut_edit', {'id': customer_files_statut.id}) }}" class="btn btn-info">Modifier</a>
        {{ include('customer_files_statut/_delete_form.html.twig') }}
    </div>
    
{% endblock %}
