{% extends 'base.html.twig' %}

{% set title = global_statut.name %}
{% block header %}
    <a href="{{ path('default') }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th># :</th>
                <td>{{ global_statut.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ global_statut.name }}</td>
            </tr>
            <tr>
                <th>Créer le :</th>
                <td>{{ global_statut.createdAt ? global_statut.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modifier le :</th>
                <td>{{ global_statut.updatedAt ? global_statut.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col mt-4">
        <div class="row">
            <a href="{{ path('global_statut_edit', {'id': global_statut.id}) }}" class="btn btn-primary">Modifier</a>
            {{ include('global_statut/_delete_form.html.twig') }}
        </div>
    </div>
    
{% endblock %}
