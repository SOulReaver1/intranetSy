<form method="post" action="{{ path('customer_source_delete', {'id': customer_source.id}) }}" onsubmit="return confirm('Attention ! Êtes-vous sûr de vouloir supprimer cet objet ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer_source.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
