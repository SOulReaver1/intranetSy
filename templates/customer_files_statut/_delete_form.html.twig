<form method="post" action="{{ path('customer_files_statut_delete', {'id': customer_files_statut.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer_files_statut.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
