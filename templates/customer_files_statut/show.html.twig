{% extends 'base.html.twig' %}

{% set title = customer_files_statut.name %}
{% block header %}
    <a href="{{ path('customer_files_statut_index', {'global': app.session.get('global')}) }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}
<div class="row">
    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ customer_files_statut.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ customer_files_statut.name }}</td>
            </tr>
            <tr>
                <th>Couleur :</th>
                <td><span class="badge" style="background-color: {{customer_files_statut.color}}">{{customer_files_statut.color}}</span></td>
            </tr>
            <tr>
                <th>Date de création : </th>
                <td>{{ customer_files_statut.createdAt ? customer_files_statut.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date de modification :</th>
                <td>{{ customer_files_statut.updatedAt ? customer_files_statut.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <div class="col mt-4">
        <div class="row">
            <a href="{{ path('customer_files_statut_edit', {'id': customer_files_statut.id, 'global': app.session.get('global')}) }}" class="btn btn-primary">Modifier</a>
            {{ include('customer_files_statut/_delete_form.html.twig') }}
        </div>
    </div>
    
{% endblock %}
