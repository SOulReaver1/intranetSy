<div class="modal fade" id="changePasswordAlModal" tabindex="-1" role="dialog" aria-labelledby="changePasswordAlModalTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
        {{ form_start(pwdForm) }}
            <div class="modal-header">
                <h5 class="modal-title" id="changePasswordAlModalTitle">Changer le mot de passe AL</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                {{ form_widget(pwdForm) }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
                <button type="submit" class="btn btn-success">Sauvegarder</button>
            </div>
        {{ form_end(pwdForm) }}
    </div>
  </div>
</div>